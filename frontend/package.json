{
  "name": "frontend",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "predev": "rm -rf .nuxt 2>/dev/null || true",
    "dev": "nuxt dev --port 9000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@types/qrcode": "^1.5.6",
    "@vueup/vue-quill": "^1.2.0",
    "bootstrap": "^5.3.8",
    "bootstrap-icons": "^1.13.1",
    "nuxt": "^4.2.1",
    "qrcode": "^1.5.4",
    "vue": "^3.5.25",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "sass": "^1.100.0",
    "sass-embedded": "^1.100.0"
  }
}
